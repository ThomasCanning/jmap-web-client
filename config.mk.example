# Web Client Deployment Configuration
# Copy this file to config.mk and edit with your values
REGION = eu-west-2
DEPLOYMENT_DOMAIN = example.com

# Deployment mode: 'separate' (own CloudFront) or 'shared' (use server CloudFront)
# This will be set automatically on first deployment
DEPLOYMENT_MODE = 

# Server CloudFront distribution ID (required when DEPLOYMENT_MODE = shared)
# Leave empty if using separate mode
SERVER_CLOUDFRONT_ID =

# Auth login endpoint path (relative to server URL)
# The client will POST username/password to this endpoint
# The server should set access_token and refresh_token cookies and return {"success":true} or {"error":"..."}
# Default: /auth/login
AUTH_LOGIN_ENDPOINT = /auth/login

